<p id="notice"><%= notice %></p>

<h1>
  <%= @user.username %>
</h1>

<% if @user == current_user %>
	<%= link_to 'Edit', edit_user_path(@user), class:"btn btn-primary", style:"color:white;" %>  </br></br>
	</br>
	<h3> I'm admin in </h3>
	<table class="table table-hover">
		<% @user.companies.each do |c| %>
			<tr><%= link_to c.name, c %> </tr>
			</br>
		<% end %>
	</table>
<% end %>

<%= link_to 'Back', users_path %>
